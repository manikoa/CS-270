#include<iostream>
using namespace std;
#include "Mars.h" // Include the "Mars.h" header file that contains the function definitions

int main()
{
    // TRY IT #1: Call the `rectangleArea` function with width 13 and height 8
    cout << rectangleArea(13, 8) << endl;

    // TRY IT #2: Call the `metricToEnglishTemperature` function with temperature 78
    cout << metricToEnglishTemperature(78) << endl;

    // TRY IT #3: Call the `YeartoHours` function with year 6
    cout << YeartoHours(6) << endl;

    return 0;
}
